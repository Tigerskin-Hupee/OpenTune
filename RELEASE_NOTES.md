[重大] 修復 App 反覆閃退問題

### 更新內容

- **修復閃退**：媒體庫掃描合併重複歌手時，資料庫更新撞到唯一性限制導致整個 App 閃退。合併操作改為安全模式（遇到重複自動跳過），並為所有背景資料庫操作加上錯誤保護——以後資料庫問題只會記錄到 Crash Log，不再讓 App 閃退。
- 包含 v2.0.5 全部功能：Crash 記錄（設定 → 關於 → Crash Log）、重大更新自動提示、下一首預解析（切歌幾乎瞬間）。
