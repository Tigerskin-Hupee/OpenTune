name: Build APKs
on:
  workflow_dispatch:
  push:
    branches:
      - canary
  pull_request:
    branches:
      - '**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive

      - name: set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: 21
          distribution: "temurin"
          cache: 'gradle'

      - name: Accept SDK licenses
        run: yes | /usr/local/lib/android/sdk/cmdline-tools/latest/bin/sdkmanager --licenses

      - name: Build debug + release APKs and lint
        run: ./gradlew assembleCoreDebug assembleCoreRelease lintCoreDebug --stacktrace -DskipFormatKtlint

      - name: Upload debug APK
        uses: actions/upload-artifact@v4
        with:
          name: OpenTune-debug
          path: app/build/outputs/apk/core/debug/*.apk

      - name: Upload release APK
        uses: actions/upload-artifact@v4
        with:
          name: OpenTune-release
          path: app/build/outputs/apk/core/release/*.apk
